export const config = {
  development: {
    dialect: "postgres",
    username: "postgres",
    password: "admin",
    database: "enaton",
    host: "localhost",
    port: 5432,
  },
  // Add other environments if needed (e.g., "test", "production")
};
